{
  "name": "versioning",
  "version": "1.8.5",
  "description": "Test Manual release version",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release-ci": "release-it --ci"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "release-it": "^16.1.5"
  }
}
